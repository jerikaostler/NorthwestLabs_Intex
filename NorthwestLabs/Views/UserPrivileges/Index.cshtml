@model IEnumerable<NorthwestLabs.Models.UserPrivileges>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Privilege.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.User.Password)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Privilege.Description)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.User.Password)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.UserPrivilegesID }) |
            @Html.ActionLink("Details", "Details", new { id=item.UserPrivilegesID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.UserPrivilegesID })
        </td>
    </tr>
}

</table>
